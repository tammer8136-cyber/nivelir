# HANDOFF: допуск угла i переведён на РЭ приборов

Дата: 29.08.2026. Продолжение HANDOFF-NORMATIVE-v2.
Патч: `patch-11-17-combined.diff`, 9 файлов, +2627 −452.

---

## 0. Главный вывод

Прежняя версия приложения выносила вердикт по единому порогу 10″
(ГОСТ 10528-90, п. 2.3) и брала допуск из каталога, где числа 3″ и 5″
были выведены по правилу ГКИНП 17-196-85 — сборника, утратившего силу
01.10.1999 и уже занесённого в код как неприменимый.

Проверка по **36 руководствам и паспортам** показала, что оба числа
неверны, а сама конструкция «единый допуск» неверна принципиально:

**Ни один РЭ не задаёт полевой допуск на угол i в угловых секундах.**
Везде миллиметры расхождения на схеме, заданной тем же РЭ. Значение 10″
встречается только в свидетельстве о поверке (УОМЗ, коллиматор УК1) и в
лабораторном разделе Leica Jogger (эталонный нивелир Н-05, экзаменатор по
ГОСТ 13012-67) — то есть в метрологии, а не в поле.

Разброс заводских допусков после пересчёта — **от 6,9″ до 44,2″**.
Прежний порог 10″ был строже любого РЭ, кроме Bosch GOL 32: приложение
браковало приборы, которые их собственные производители признают годными.

---

## 1. Формула и её подвох

Отсчёт по рейке равен истинному плюс i·d, где d — плечо. На станции с
равными плечами ошибка сокращается, на смещённой остаётся:

    i = Δ / ΔL,    Δ = (a2 − b2) − (a1 − b1),    ΔL = d(A₂) − d(B₂)

    i″ = 206265 · Δ[мм] / ΔL[мм]

**Миллиметры в РЭ намерены не на базе, а на РАЗНОСТИ ПЛЕЧ.** База в РЭ
почти всегда задана диапазоном (30–50, 40–70, 80–90 м), поэтому у одного
и того же числа нет одного угла: 3 мм при ΔL 26 м это 23,8″, при 48 м —
12,9″. Пересчёт без указания ΔL бессмыслен.

Вилка внутри одного РЭ доходит до ×1,85 (RGK: 12,9–23,8″). Приложение
берёт **строгий край** — максимальную разрешённую ΔL. Это решение
исполнителя, а не производителя: РЭ вилку не оговаривает. Оба края
печатаются в протоколе.

Соотношение величин, из-за которого выбор края решили не драматизировать:
разброс между приборами ×6,4, ошибка старого каталога ×4,3, вилка ×1,85.

---

## 2. Три схемы расстановки

**mid_then_near** (65 карточек). Станция 1 посередине, станция 2 ВНУТРИ
отрезка вплотную к рейке. ΔL = база − 2·вынос. Положение внутри отрезка
сверено по рисункам РЭ RGK C (рис. 10), RGK N (рис. 9), GEOBOX N7 и
Sokkia B (рис. 7.6) — текст этого не говорит, решает картинка.

**mid_then_beyond** (2 карточки, УОМЗ). Станция 2 ЗА передней рейкой в
2–4 м. Ближнее плечо = вынос, дальнее = база + вынос, поэтому **ΔL = база**
и от выноса не зависит.

**thirds_symmetric** (2 карточки, Leica NA2/NAK2). Отрезок 45–60 м делится
на ТРИ равные части, рейки в точках ⅓ и ⅔, станции на концах. Посередине
не стоят вообще. Плечи d/2d и 2d/d, угол входит с разными знаками, поэтому
**ΔL = 2d = ⅔ общей длины**. Взять ΔL равной базе — ошибка в полтора раза.

**Схемы 50/50 + 25/75 нет ни в одном РЭ.** Это методика исполнителя, и
провенанс у неё другой.

---

## 3. Четыре степени полноты РЭ

Все четыре реально встречаются, и модель обязана их различать.

| состояние | пример | что заполнено |
|---|---|---|
| схема + база + допуск | RGK, Vega, Leica NA/Jogger, УОМЗ, Nivel System | всё |
| схема + база, допуска нет | Topcon AT-B/AT-G, Sokkia B | `tolerance_mm = null` |
| только схема | ADA Basis / Prof / Ruber | база и допуск пусты |
| допуск без геометрии | GeoMax ZAL300 (руководство в пиктограммах) | база и вынос пусты |

Формулировка второй группы дословно: «юстируйте, пока разность не станет
малой». Числа нет вовсе.

**Пропуск нельзя заполнять по прибору-близнецу.** Sokkia B и Topcon AT-B
совпадают дословно вплоть до нумерации разделов (7.1 круглый уровень,
7.2 компенсатор, 7.3 сетка нитей), одинаковые корпус 215 мм, микрометр OM5
и насадка DE16/DE22 — но базу 30–50 м называет только Topcon (и английская
редакция Sokkia, см. п. 6).

---

## 4. Что сделано в коде

**`lib/models/field_check_spec.dart`** (новый). Схема, база, вынос, допуск
в мм, оба края ΔL, источник. Всё кроме схемы и источника — nullable.
`geometryWithinRe` возвращает `bool?`: третье значение — «в РЭ нет базы,
проверять не на чем», это не «нет».

**`lib/services/nivelir/applied_tolerance.dart`** (новый). Разрешает
основание вердикта в три ветки:

1. `reMillimetres` — РЭ даёт число И расстановка в геометрии РЭ. Сравнение
   в миллиметрах, ровно как у производителя, пересчёт в вердикт не входит.
2. `reConverted` — число есть, геометрия своя либо базы нет. Перенос в
   секунды по строгому краю, с указанием причины в провенансе.
3. `executor` — числа нет. Задаёт исполнитель, протокол печатает, чего
   именно не хватило.

**Отката на 10″ нет ни в одной ветке.**

**`collimation.dart`.** `summarize` принимает `AppliedTolerance` вместо
`double`. Добавлено `deltaMm` — среднее `xMm` по приёмам. Оказалось, что
`xMm` в `computeRun` это и есть `(a2−b2)−(a1−b1)`, то есть ровно та
величина, которую РЭ сравнивает с миллиметрами: считать ничего нового
не потребовалось. `toleranceArcsec` остался производным геттером, поэтому
17 мест в UI и экспорте компилируются без правок.

**`device_model.dart`.** Поле `fieldCheck`, геттеры `hasToleranceFromRe`
и `toleranceArcsecFromRe`. Старый `toleranceArcsec` помечен `@Deprecated` —
компилятор покажет все места, которые ещё живут на десяти секундах.

**`database_service.dart`.** Схема v7: девять колонок `field_check_*`,
миграция с переносом экземпляров по паре brand+model.

**`wizard_state.dart`.** `needsExecutorTolerance` и
`setExecutorToleranceArcsec`. База считается как сумма плеч станции 1,
вынос — меньшее плечо станции 2.

**`help_articles.dart`.** 13 статей вместо 12, полностью переписаны статьи
про допуск и схемы, добавлена «Откуда берутся числа» с рангом источников.

**`test/collimation_test.dart`.** Новая группа «основание допуска», 5 тестов.

---

## 5. Каталог: 87 карточек

Было 53. Стало 87: **59 с допуском из РЭ, 10 со схемой без числа,
18 без данных**.

Удалено поле `i_angle_tolerance_arcsec` и правило по 17-196-85 из `_meta`
(в коде поле не читалось — миграции не потребовалось).

Каталог собирается скриптом `build_catalog.py`, а не правится руками.
Блок `SPEC_FIXES` в нём — точечные правки характеристик по первоисточнику,
каждая с причиной.

**Не заведён: УОМЗ 3Н-5Л** — уровенный нивелир с цилиндрическим уровнем и
элевационным винтом, компенсатора нет (19 упоминаний уровня, ноль
компенсатора; описание типа 2018 г. подтверждает). Вне области приложения,
хотя методика в паспорте полная.

**Не заведён: Geobox N7-36** — методика есть (40–50 м, 1–2 м, 3 мм), но в
РЭ нет таблицы характеристик, а `sko_mm_km NOT NULL`. Выдумывать СКО не
стали.

---

## 6. Ошибки, найденные попутно

**Редакция РЭ решает.** У Sokkia русский перевод базу опускает, английский
пишет 30–50 м. У Leica Jogger русская редакция содержит только
лабораторную поверку, а полевая методика (п. 3.2, 30 м / 1 м / 3 мм) есть
в других языковых редакциях. Дважды подряд ответ находился в другой
редакции того же документа.

**Topcon AT-G1: минимальный фокус 1,0 м, а не 0,5.** В каталоге стояло
0,5 — это значение моделей G3/G4/G6.

**Минимальный фокус ведётся без точки отсчёта.** Спецификация Sokkia
B-серии: «0.2 m from end of telescope, 0.3 m from instrument center». То
есть 0,2 у Sokkia и 0,3 у Topcon — одно число в двух точках отсчёта, а не
описка. У Sokkia и Topcon точка отсчёта теперь в `min_focus_note`;
**у остальных 79 карточек она неизвестна.**

**Разночтение в редакциях CST/Berger.** Bosch-овский Original Instructions
Manual даёт вынос 1–2 м, CST-брендированный — 2 м, при одинаковых базе
30–50 м и допуске 3 мм. В каталоге 1–2 м.

---

## 7. Правовой статус ГКИНП — уточнён

Механизм — **ч. 5 ст. 32** Федерального закона от 30.12.2015 № 431-ФЗ
(порядок вступления в силу), НЕ ст. 31: та про законодательные акты.

Текст: положения принятых до дня вступления закона в силу нормативных
актов органов государственной власти СССР, РСФСР и Российской Федерации,
регулирующие отношения в сфере геодезии и картографии, **действуют до
1 января 2018 года в части, не противоречащей** этому закону. Закон вступил
в силу 01.01.2017.

Читать буквально: закон никого не «признаёт утратившим силу», он ставит
срок и добавляет оговорку. Отдельного приказа об отмене ГКИНП не
существует. Пометка справочно-правовых систем «фактически утратил силу в
связи с принятием 431-ФЗ» — вывод составителей базы, а не цитата.

На приложение не влияет: цепочка идёт СП 317 п. 4.12 → эксплуатационная
документация прибора → СТО исполнителя (СП 47.13330, пп. 4.9 и 4.19),
и ГКИНП в неё не входят ни при каком прочтении.

---

## 8. Что дальше

**Блокирует релиз:**

- Блок провенанса в PDF и Excel. `AppliedTolerance.provenance` готов,
  осталось довести до `export_service` и `excel_export_service`.
- Мастер должен спрашивать допуск, когда `needsExecutorTolerance` истинно
  (28 карточек из 87), и не давать вынести вердикт без числа.
- `Verification` не снимает основание вердикта: нужны
  `tolerance_basis_snapshot` и `tolerance_provenance_snapshot` рядом с
  существующим `tolerance_arcsec_snapshot`.
- Мигрировать 17 мест UI с `toleranceArcsec` на `tolerance`; снять
  `@Deprecated`.
- Разобрать `RunsNorm` — сейчас это выбор между двумя ГКИНП, который
  пишется в протокол как основание. Становится настройкой «число приёмов»
  без нормативной подписи.
- Периодичность и табл. 4 в `leveling_class.dart` — пометить справочными.

**Не блокирует:**

- 18 карточек без данных РЭ. Приложение корректно печатает «источник не
  установлен», добивать можно фоном.
- Разделить карточку GeoMax «ZAL100 / ZAL120»: руководство ZAL 120/124
  покрывает вторую половину, первую нет.
- CST/Berger PAL32 — в руководство серии не входит, нужен отдельный
  документ.
- Ревизия справки. Отложена намеренно до конца расчётной части: статьи
  про допуск и схемы придётся переписывать под неё. Делить по адресату —
  «что делать сейчас» против «на каком основании», через поле `kind`
  в `HelpArticle`.

**Как искать оставшиеся РЭ.** Manualslib и Scribd блокируют
автоматический доступ, но в браузере работают и отдают постраничный
просмотр с выбором языка. Прямые PDF ищутся по номеру документа, а не по
названию (так нашёлся Topcon AT-B, `FC10386-A011-01`). Пересказы на сайтах
магазинов не годятся: в протокол идёт пункт документа, а у пересказа
пункта нет.

---

## 9. Не проверено

Dart SDK в контейнере нет — код не компилировался. Проверено статически:
баланс скобок, наличие каждой колонки в CREATE / ALTER / SELECT / модели /
JSON, отсутствие разыменований nullable без проверки, пересчёт всех 59
значений допуска заново по формуле (расхождений нет), инварианты каталога
(нет пересчёта без исходных данных, нет источника без схемы, нет выноса
у схемы третей).

`flutter analyze` и тесты гонять на машине. `@Deprecated` даст
предупреждения ровно в тех местах, которые перечислены в п. 8.
